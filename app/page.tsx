import { Button } from "@/components/ui";

export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <Button size="lg">Hello! This application dev environment is ready to work</Button>
    </main>
  );
}
